# CubeLV 插件鍛造

你的任務是建立或修改一個 CubeLV 插件。

## 第一步：完整閱讀範例插件（禁止跳過）

**在寫任何程式碼之前，你必須完整讀完以下兩個內建插件的所有檔案。不是讀一個檔案，是每個插件目錄下的每一個檔案都讀。**

1. `{VAULT_SHARED}/plugins-src/plugins/note/` — 完整讀目錄下所有 `.ts`、`.tsx`、`.css` 檔案
2. `{VAULT_SHARED}/plugins-src/plugins/classify/` — 完整讀目錄下所有 `.ts`、`.tsx`、`.css` 檔案

讀完後你就知道：Plugin 怎麼寫、View 怎麼寫、Store 怎麼寫、CSS 怎麼寫、registerPluginClass 怎麼呼叫。照著做就對了。

## 模組 import

直接用 `import`，esbuild 自動處理：

```tsx
import React, { useState, useEffect } from 'react';
import { Plugin, ReactView, registerPluginClass, useItemStore } from '@cubelv/sdk';
```

第三方 npm 庫也直接 `import`，esbuild 會打包進 bundle。需要時先安裝：
```bash
cd /absolute/path/to/plugins/{pluginName}
npm init -y && npm install <package>
```

## 寫入規則

- 所有檔案寫在 `plugins/{pluginName}/`，入口 `main.tsx`
- `pluginName` 英文小寫 + 連字號
- 檔案末尾必須呼叫 `registerPluginClass(MyPlugin, '名稱')`
- CSS class 用插件名稱前綴（如 `.pomodoro-timer`）
- `.map()` 渲染的 JSX 必須有 `key`
- `onunload()` 清理 `onload()` 插入的 DOM（如 `<style>`）
- **禁止在 `plugins/` 以外寫入**

## 安全限制

1. `{VAULT_SHARED}/plugins-src/` 唯讀，禁止修改
2. 只能在 `plugins/{pluginName}/` 寫入
3. Bash 只用絕對路徑
