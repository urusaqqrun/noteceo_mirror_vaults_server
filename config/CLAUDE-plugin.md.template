# CubeLV 插件鍛造

你的任務是建立或修改一個 CubeLV 插件。

## 第零步：先確認目標目錄與 codeStem

### 工作目錄（重要）

- 你的 **CWD 就是 vault 根目錄**（例如 `/vaults/abc123/`）
- 唯一允許寫入的路徑是 `plugins/<系統提供的目錄名>/`（相對於 CWD）
- **直接 `mkdir -p plugins/<目錄名>` 建立目錄**，不要浪費工具呼叫去 `ls /`、`find /`、`pwd`、`ls /app/`、`ls /home/` 探索容器環境
- **禁止讀取或存取 `plugins/` 下其他插件的目錄或檔案**（那些可能是失敗產物）
- **禁止存取 vault 根目錄以外的任何路徑**

### 目錄與 codeStem

- 用戶訊息第一行會明確給你目標目錄，例如：`插件目錄名稱：plugins/番茄鐘插件-cd1677/`
- **必須使用系統提供的目錄名，不得自行改目錄名、另建第二個插件目錄、或把檔案寫到其他名稱的目錄**
- 在寫任何檔案前，先自行選定一個固定的 **ASCII `codeStem`**
- `codeStem` 只可使用英文字母、數字、連字號，整個插件都必須固定使用同一個 `codeStem`
- `codeStem` 用途必須統一：
  - React 元件 / class 命名：由 `codeStem` 衍生成 PascalCase，例如 `pomodoro` → `PomodoroPlugin.tsx`
  - 檔名 stem：例如 `pomodoroStore.ts`、`pomodoro.css`
  - CSS class prefix：例如 `.pomodoro-*`
  - localStorage key prefix：例如 `plugin:pomodoro:*`
- **目錄名可以是 Unicode；`codeStem` 必須是 ASCII。不要把兩者混為一談**

## 第一步：了解現有插件生態

**Read `{VAULT_SHARED}/plugins-src/PLUGINS_INDEX.md`**

這份索引是部署時自動從原始碼生成的，列出所有內建插件的名稱、功能、依賴、每個檔案的說明。讀完後你應該知道系統裡有哪些插件、各自做什麼。

## 第二步：判斷插件類型並讀取對應參考

根據用戶需求判斷要做的是哪種類型，讀取對應的參考文件：

- **新增獨立功能**（有自己的 sidebar + view + store）→ Read `{VAULT_SHARED}/plugins-src/REFERENCE_STANDALONE.md`
- **加強/擴展現有功能**（猴子補丁、注入 UI）→ Read `{VAULT_SHARED}/plugins-src/REFERENCE_PATCH.md`
- 兩者兼具 → 兩份都讀

參考文件包含：完整 Plugin API 簽名、CSS 變數列表、完整最小範例、交付 Checklist。

## 第三步：按需查找細節（可選）

如果參考文件不夠，根據 PLUGINS_INDEX.md 中的檔案說明，自行決定讀取哪個內建插件的原始碼。
大多數情況下不需要這步。

**禁止讀取用戶 vault 裡 `plugins/` 下的其他插件目錄。** 那些可能是之前失敗的產物。只能讀取 `{VAULT_SHARED}/plugins-src/` 內的參考檔案。

## 第四步：先判斷主要掛載方式，再開始寫碼

在寫任何檔案前，先根據需求選定**一個主要掛載方式**：

- **`registerSidebarWithView`（首選）**：插件有自己的畫面且需要 sidebar 入口 — **一次呼叫完成 sidebar + view + 導航 + pane 掛載**
- `registerOverlay`：插件主要是對話框、選單、浮層
- `addCommand`：插件主要是指令或快捷鍵觸發
- `registerWindowView`：插件主要是在獨立視窗開啟

規則：
- **有自己畫面的插件必須用 `registerSidebarWithView`**，禁止手動拆開成 registerView + registerSidebarSection + subscribe + ensureMounted
- 若插件是猴子補丁 / 注入型 UI，通常不應搶占 center pane
- **每個插件都必須有至少一個「使用者不需要記快捷鍵就能操作」的可見入口**。如果主要是 overlay，至少要搭配 sidebar section 或一個常駐 FAB 按鈕。只有 `addCommand` 快捷鍵而沒有其他入口是不合格的。

## Plugin 基類 API

所有 register 系列方法在 `onunload()` 時自動清理：

| 方法 | 用途 |
|------|------|
| **`registerSidebarWithView(config)`** | **一次完成 sidebar + view + 導航 + pane 掛載（首選）** |
| `registerView(type, creator)` | 註冊 View 類型（低階，通常不直接用） |
| `registerOverlay(id, Component)` | 註冊全局浮動元件（dialog / menu） |
| `registerWindowView(itemType, viewType, opts, onOpen?)` | 宣告 itemType 可開獨立視窗 |
| `registerEditorExtension(id, ext)` | 註冊 TipTap 編輯器擴展 |
| `registerSidebarSection(desc)` | 註冊側欄區塊（低階，通常不直接用） |
| `registerSearchProvider(itemType, provider)` | 註冊搜尋 |
| `registerItemFields(itemType, ext)` | 擴展 itemType schema 欄位 |
| `registerAIHints(itemType, hints)` | 註冊 AI 操作提示 |
| `registerAISkill({ name, instruction })` | 註冊 AI 技能指令 |
| `addCommand(cmd)` | 註冊指令（含快捷鍵） |
| `registerEvent(event, handler)` | 訂閱 eventBus 事件 |
| `registerDomEvent(el, type, callback)` | 訂閱 DOM 事件 |
| `registerInterval(id)` | 註冊 setInterval |
| `register(cleanup)` | 註冊任意清理函式 |
| `clearPane(paneId)` | 清空 pane 內所有 leaf |
| `ensureLeafInPane(paneId, leafId, viewType, sizing)` | 確保 pane 中有指定 leaf（冪等） |

## Workspace 面板結構與觸發鏈路（最重要 — 不做就等於白寫）

### 面板結構

```
┌─────────┬──────────────┬──────────────┬──────────┐
│ leftPane│ centerPane1  │ centerPane2  │ rightPane│
│ (sidebar)│ (列表)       │ (詳情/編輯器) │ (AI 面板) │
└─────────┴──────────────┴──────────────┴──────────┘
```

每個插件「接管」center 區域的方式：
- **雙欄插件**（note, todo, chart）：`centerPane1` 放列表，`centerPane2` 放編輯器/詳情
- **單欄插件**（card, auto-task）：`clearPane('centerPane1')` 清空列表區，`centerPane2` 放主視圖

### registerSidebarWithView — 有畫面的插件必用

一次呼叫完成 sidebar + view 註冊 + 導航訂閱 + pane 掛載，不需要手動串接：

```typescript
// 雙欄範例（列表 + 詳情）
this.registerSidebarWithView({
    sidebar: {
        id: 'my-plugin',
        title: i18next.t('myPlugin'),
        orderAt: 600,
        useData: useMyPluginSidebarData,
    },
    views: [
        { type: 'my-list', creator: (leaf) => new MyListView(leaf) },
        { type: 'my-detail', creator: (leaf) => new MyDetailView(leaf) },
    ],
    activateOnItemTypes: ['MY_PLUGIN_FOLDER'],
    panes: {
        centerPane1: { leafId: 'leaf-mylist', viewType: 'my-list', size: 350, minSize: 240 },
        centerPane2: { leafId: 'leaf-mydetail', viewType: 'my-detail', flex: 1, minSize: 360 },
    },
});

// 單欄範例（僅主視圖）
this.registerSidebarWithView({
    sidebar: {
        id: 'pomodoro',
        title: i18next.t('pomodoro'),
        orderAt: 700,
        useData: usePomodoroSidebarData,
    },
    views: [
        { type: 'pomodoro-timer', creator: (leaf) => new PomodoroTimerView(leaf) },
    ],
    activateOnItemTypes: ['POMODORO_FOLDER'],
    panes: {
        centerPane1: 'clear',
        centerPane2: { leafId: 'leaf-pomodoro', viewType: 'pomodoro-timer', flex: 1, minSize: 360 },
    },
});
```

**View class 定義方式：** 繼承 `ReactView`，覆寫 `getViewType()`、`getDisplayText()`、`renderComponent()`：
```typescript
class MyListView extends ReactView {
    getViewType() { return 'my-list'; }
    getDisplayText() { return '我的列表'; }
    renderComponent() { return <MyList />; }  // 注意是 renderComponent 不是 render
}
```

**自動行為：** 用戶點擊 sidebar item → `activateOnItemTypes` 匹配 → 自動把 View 掛到指定 pane。不需要手動寫 `subscribe`、`ensureMounted`、`ensureLeafInPane`。

**useData hook 的 sidebar item 規範：** 所有 item 的 `itemType` 必須以 `_FOLDER` 結尾（如 `POMODORO_FOLDER`），否則不會在 sidebar 顯示。

**注意：** 猴子補丁類插件（如 classify）不搶 center pane，改用 `MutationObserver` + `createPortal` 注入 UI，不需要 `registerSidebarWithView`。

### 常見錯誤（禁止）

- 手動拆開寫 `registerView` + `registerSidebarSection` + `subscribe` + `ensureMounted` — 必須用 `registerSidebarWithView`
- 寫死 item ID / memberID / folderID / plugin ID
- `registerOverlay` 但沒有任何**可見的**觸發入口（只有 `addCommand` 快捷鍵不算可見入口）
- 靜態 CSS 用手動 `<style>` 注入
- 整個插件載入後 UI 完全不可見 — 至少要有 sidebar section、FAB 按鈕、或其他常駐可見元素

## 檔案結構（必須拆分，禁止全部塞進一個檔案）

插件必須拆分為多個檔案，和內建插件完全一樣的結構。**禁止把所有邏輯寫在一個檔案裡。**

拆分原則（照抄內建插件的做法）：
- **{CodeStemPascal}Plugin.tsx**：入口檔案，Plugin 子類 + `registerPluginClass`，只做註冊，不放 UI
- **每個 React 元件一個 `.tsx` 檔案**：如 `{CodeStemPascal}List.tsx`、`{CodeStemPascal}Panel.tsx`
- **CSS 獨立 `.css` 檔案**：例如 `{codeStem}.css`，不要用 inline style
- **Store 獨立 `.ts` 檔案**：例如 `{codeStem}Store.ts`，用 Zustand
- 檔案命名照內建插件慣例，不要自己發明

## 寫入規則

### 基本

- 所有檔案寫在**系統提供的目標目錄**內
- 不得改動、重命名、搬移系統提供的插件目錄
- 元件名、檔名 stem、CSS prefix、localStorage key 一律使用同一個 `codeStem`
- 入口檔案末尾必須呼叫 `registerPluginClass(MyPlugin, '名稱', options)`，**options 必須包含 `descriptionKey` 和 `setupI18n`**：
```typescript
import { registerPluginClass, registerPluginLocales } from '@cubelv/sdk';
registerPluginClass(PomodoroPlugin, '番茄鐘', {
    descriptionKey: 'pluginPomodoroDesc',
    setupI18n: () => registerPluginLocales('PomodoroPlugin', {
        'zh-Hant': { pluginPomodoroDesc: '25 分鐘專注計時，搭配短休息與長休息循環' },
        'en': { pluginPomodoroDesc: '25-min focus timer with short/long break cycles' },
    }),
});
```
  `descriptionKey` 會顯示在「插件管理面板」的插件說明區域，**不寫的話使用者看到的插件列表就沒有任何說明**

### SDK / Import 規則

- 所有框架 API（`Plugin`、`ReactView`、`registerPluginClass`、`useItemStore`、`i18next` 等）一律從 `@cubelv/sdk` import
- **國際化只能 import `i18next`，禁止 import `i18n`**。宿主 SDK 暴露的是 `i18next`，若寫成 `import { i18n } from '@cubelv/sdk'`，插件會在 `onload()` 時直接 crash
- **禁止 `require()`** — 只能用 ES module `import`
- **禁止 `import React from 'react'`**（本環境使用 `jsx: 'automatic'`）
- 允許的 import 來源只有三種：
  1. `@cubelv/sdk` — 框架 API
  2. 相對路徑 `'./xxx'` — 同插件內的檔案
  3. `package.json` 中 `dependencies` 宣告的第三方庫
- 沒有宣告在 `dependencies` 的第三方庫不能 import

### React 資源清理（重要 — 不做會造成記憶體洩漏與重複執行）

- `useEffect` 中啟動的 `setInterval` / `setTimeout` 必須在清理函式中 `clearInterval` / `clearTimeout`
- `useEffect` 中建立的 `AudioContext` 必須在清理函式中 `.close()`
- `useEffect` 中加的 `addEventListener` 必須在清理函式中 `removeEventListener`
- **反面範例（禁止）：**
```typescript
useEffect(() => {
    const id = setInterval(tick, 1000);
    // 沒有 return cleanup — 切換頁面後 interval 仍在跑
}, []);
```
- **正確做法：**
```typescript
useEffect(() => {
    const id = setInterval(tick, 1000);
    return () => clearInterval(id);
}, []);
```
- 插件層級（非 React 元件內）的 interval 用 `this.registerInterval(setInterval(fn, ms))` 讓框架在 `onunload()` 時自動清理
- 其他需要清理的資源用 `this.register(() => { /* cleanup */ })`

### 狀態持久化

- 插件內部設定（如用戶偏好、上次選中項等）使用 `localStorage`
- key 格式必須是 `plugin:<codeStem>:<名稱>`，例如 `plugin:pomodoro:workMinutes`
- 讀取時必須 `try-catch`，解析失敗就用預設值：
```typescript
function loadSetting<T>(key: string, defaultValue: T): T {
    try { return JSON.parse(localStorage.getItem(key) ?? '') ?? defaultValue; }
    catch { return defaultValue; }
}
```
- **禁止使用 IndexedDB、WebSQL、或 fetch 外部 API 儲存資料**

### CSS 與主題（重要 — 不用 token 在深色模式會全部壞掉）

- 一般樣式一律使用 `import './x.css'`
- **不要手動注入 `<style>`**，除非樣式內容是在執行期動態生成；若真的手動插入，必須在 `onunload()` 清理
- CSS class 用 `codeStem` 當前綴，例如 `.pomodoro-timer`
- **禁止任何硬編碼顏色值**（`#fff`、`#333`、`rgb(...)`、`rgba(...)`），必須使用 CSS 變數：
  - 文字顏色：`var(--on)`
  - 文字次要：`color-mix(in srgb, var(--on) 75%, transparent)`
  - 文字第三層：`color-mix(in srgb, var(--on) 50%, transparent)`
  - 背景色：`var(--background)` / `var(--surface)`
  - 主色：`var(--primary)`
  - 強調色：`var(--accent)`
  - 錯誤色：`var(--error)`
  - 高亮色：`var(--highlight)`
  - 邊框/分隔線：`1px solid color-mix(in srgb, var(--on) 8%, transparent)` 或 `16%`
  - 陰影：`box-shadow: 0 2px 8px color-mix(in srgb, var(--on) 10%, transparent)`
- **hover 效果規則：** opacity 顯隱（0→1）必須包在 `@media (hover: hover)` 中；color/background-color/transform 等樣式變化不需包在 media query
- 彩色文字做法：`background: var(--rainbow); -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent;`

### 國際化 i18n（重要 — 所有使用者可見文字都必須翻譯）

- **所有 UI 文字**（按鈕文字、標題、placeholder、tooltip、提示訊息、aria-label）一律使用 `i18next.t('key')`
- **禁止任何硬編碼的使用者可見字串**，包括看起來「只有英文應該沒問題」的文字（如 `"Start"`、`"Settings"`）
- 入口檔案若需要標題、sidebar 名稱、`getDisplayText()`，也必須使用 `i18next.t(...)`
- 翻譯必須同時提供 **`zh-Hant`、`en`、`ja`** 三個語系，在 `setupI18n` 中用 `registerPluginLocales` 註冊
- **反面範例（禁止）：**
```tsx
<button>開始</button>
<button>Start</button>
```
- **正確做法：**
```tsx
<button>{i18next.t('start')}</button>
```
並在 `setupI18n` 中：
```typescript
registerPluginLocales('PomodoroPlugin', {
    'zh-Hant': { start: '開始' },
    'en': { start: 'Start' },
    'ja': { start: '開始' },
});
```
- 數字、純標點符號、codeStem 不需翻譯

### 其他禁令

- `.map()` 渲染的 JSX 必須有 `key`
- **禁止硬編碼任何 24 位 hex 字串**
- **禁止硬編碼 memberID、folderID、plugin ID**
- **禁止 banner 註解**（例如 `// ===== xxx =====`）
- **禁止使用 emoji**：所有文字、標題、按鈕、log 一律不准出現 emoji 字元
- **禁止在 `plugins/` 以外寫入**

## 編譯（必做，在停止前執行）

寫完所有檔案後，你必須自己執行 esbuild 編譯。如果編譯失敗，修正代碼後重新編譯，直到成功為止。

**嚴禁直接呼叫 `npx esbuild`、`esbuild` CLI 或自己寫 esbuild script。**
必須使用下方指定的包裝腳本，它會自動處理 format=iife、external singleton（react/react-dom/@cubelv/sdk）、JSX automatic 等設定。

步驟：

1. 如果有 package.json，先安裝依賴：
```bash
cd plugins/{你的插件目錄}/ && npm install --production --no-audit --no-fund
```

2. 找到入口檔（`*Plugin.tsx`），**必須**用包裝腳本編譯：
```bash
node /app/config/esbuild-plugin-bundle.mjs plugins/{你的插件目錄}/{CodeStem}Plugin.tsx plugins/{你的插件目錄}/bundle.js
```

3. 如果 esbuild 報錯，閱讀錯誤訊息，修正代碼，然後重新執行步驟 2
4. 確認 `bundle.js` 已生成
5. 驗證 bundle 格式正確（前端用 eval 載入，不支援 ES module）：
```bash
grep -c '^import ' plugins/{你的插件目錄}/bundle.js
```
結果必須為 `0`。若不為 0，代表你沒有使用包裝腳本，回到步驟 2 重新編譯。
6. 最後執行共享驗證器（這也是 hook 與後端註冊前會使用的同一套規則）：
```bash
bash /app/config/plugin-validator.sh plugins/{你的插件目錄}
```

## 完成前自檢（必做 — 逐項執行，不可跳過）

在結束前，**必須用 `grep` 或重新讀取檔案**逐項驗證以下所有項目。每項後方標注了可用的檢查方式。
其中 `/app/config/plugin-validator.sh` 是最終機器驗證來源；若共享驗證器失敗，代表不能結束也不能註冊。

### A. 結構與註冊

- [ ] 入口檔案中有且僅有一次 `registerPluginClass(...)` 呼叫 → `grep registerPluginClass *.tsx`
- [ ] `registerPluginClass` 的 options 包含 `descriptionKey` 和 `setupI18n`
- [ ] 有畫面的插件使用了 `registerSidebarWithView`，沒有手動寫 `subscribe` + `ensureMounted`
- [ ] 有至少一個不依賴快捷鍵的可見入口
- [ ] 不存在 `main.tsx` → `ls main.tsx` 應回傳找不到
- [ ] **`bundle.js` 已成功生成**（上面的編譯步驟已通過）
- [ ] **`bundle.js` 不含 ES module 語法** → `grep -c '^import ' bundle.js` 結果為 0

### B. 禁用模式掃描

對插件目錄執行以下 grep，每項結果必須為 0 筆：

- [ ] `grep -rn 'import React from' *.tsx *.ts` → 0 筆
- [ ] `grep -rEn "import\\s*\\{[^}]*\\bi18n\\b[^}]*\\}\\s*from '@cubelv/sdk'" *.tsx *.ts` → 0 筆
- [ ] `grep -rn '// =====' *.tsx *.ts` → 0 筆
- [ ] `grep -rEn '#[0-9a-fA-F]{3,8}' *.css` → 0 筆（CSS 中無硬編碼顏色）
- [ ] `grep -rEn "rgb\(|rgba\(" *.css` → 0 筆（CSS 中無 rgb/rgba）
- [ ] `grep -rn 'require(' *.tsx *.ts` → 0 筆

### C. React 品質

- [ ] 所有 `.map(` 的 JSX 都有 `key` prop → `grep -n '\.map(' *.tsx` 逐一確認
- [ ] 所有 `useEffect` 中的 `setInterval` / `setTimeout` 都有對應的清理函式
- [ ] 所有 `useEffect` 中的 `addEventListener` 都有對應 `removeEventListener`
- [ ] 若使用 `AudioContext`，清理函式有 `.close()`

### D. 國際化

- [ ] 所有 `.tsx` 中沒有硬編碼的使用者可見中文/英文/日文字串 → 全部使用 `i18next.t()`
- [ ] `setupI18n` 中同時提供 `zh-Hant`、`en`、`ja` 三種語系
- [ ] `registerPluginLocales` 中的 key 與元件中 `i18next.t()` 使用的 key 完全對應

### E. CSS 主題

- [ ] 所有顏色都使用 `var(--on)`、`var(--surface)`、`var(--background)`、`var(--primary)`、`var(--accent)` 等 CSS 變數
- [ ] hover opacity 效果包在 `@media (hover: hover)` 中

### F. 持久化與安全

- [ ] `localStorage` 的 key 都以 `plugin:<codeStem>:` 為前綴
- [ ] `localStorage` 讀取有 `try-catch` 包裹
- [ ] 只寫入系統指定的插件目錄，沒有寫到 `plugins/` 以外
- [ ] 沒有硬編碼的 24 位 hex 字串、memberID、folderID、plugin ID
- [ ] 沒有 emoji 字元

### G. 依賴

- [ ] 所有用到的第三方庫都寫在 `package.json` 的 `dependencies` 中
- [ ] 沒有多餘未使用的 dependencies

## 安全限制

1. 只能在系統指定的插件目錄內寫入，禁止寫到 `plugins/` 以外
2. Bash 指令只在插件目錄內執行，禁止 `cd /`、`ls /`、`cat /etc/` 等探索容器的操作
3. 禁止讀取其他插件的目錄
4. 禁止 `fetch` / `XMLHttpRequest` 外部 URL
5. 禁止 `eval()`、`new Function()`
